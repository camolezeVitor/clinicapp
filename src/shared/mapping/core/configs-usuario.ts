export interface ConfiguracoesDoUsuario {
    darkMode: boolean,
    sideBarInteligente: boolean,
    pressetDefinido: number | null,
    corDeIdentificacao: string,
    estiloDeAgendaPadrao: string,
    configuracaoInicialDoLayoutPorIds: Array<string>;
}